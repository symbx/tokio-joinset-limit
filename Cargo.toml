[package]
name = "tokio-joinset-limit"
version = "0.1.0"
edition = "2024"

[dependencies]
tokio = { version="1.49", default-features=false, features=["rt"] }

[dev-dependencies]
tokio = { version="1.49", default-features=false, features=["rt", "macros", "time"] }
